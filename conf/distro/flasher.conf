DISTRO_NAME = "Toradex Flasher"
DISTRO_VERSION = "1.0"

# Minimal feature set: we only need to build a bootloader, not a full OS.
DISTRO_FEATURES = "largefile"
DISTRO_FEATURES_BACKFILL_CONSIDERED = "pulseaudio bluez5 gobject-introspection-data"

DISTROOVERRIDES .= ":tdx-distro"

TCLIBC = "glibc"
